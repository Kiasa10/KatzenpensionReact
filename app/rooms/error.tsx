"use client";
import PageError from "@/components/PageError/PageError";

export default function Error() {
  return <PageError header="Ein Fehler ist aufgetreten!" text="Räume konnten nicht geladen werden. Bitte versuchen Sie es später noch einmal." />;
}
